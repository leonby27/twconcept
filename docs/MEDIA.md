# Медиа-реестр

Фото и иллюстрации генерирует/отдаёт владелец проекта отдельно. Здесь — что нужно, в каком виде и для какого блока. При вёрстке блока ставим заглушку и добавляем/уточняем строку здесь.

## Конвенция заглушек

- В разметке: `<div class="media-placeholder" data-media="<id>">` с подписью что здесь будет.
- Стиль заглушки: фон `--color-surface`, рамка дашем, по центру — название слота. (Класс добавим при первом использовании.)
- Файлы кладём в `public/assets/photos/` (фото) и `public/assets/illustrations/`, имя = id слота.
- Форматы: фото — WebP/AVIF + fallback JPEG, 2x под ретину; иллюстрации — SVG, если возможно.
- Рендер через `next/image` (фото) с шириной/высотой из таблицы.

## Слоты

| id | Блок | Тип | Аспект / размер | Бриф | Статус |
|---|---|---|---|---|---|
| `panel-screenshot` | Панель управления | скриншот/мокап | 16:10, ~1200×750 (контейнер ~660px шириной, отдавать 2x) | панель Timeweb в браузерном фрейме, светлая тема, видны ключевые разделы; владелец делает графику сам | 🟡 заглушка сверстана, ждём ассет |
| `migration-illustration` | Бесплатный перенос | — | — | слот закрыт: блок переработан, справа теперь оффер-карточки вместо декора — иллюстрация не нужна | ✖ закрыт |
| `datacenter-photo` | Инфраструктура | фото | 3:2, ~1200×800 | реальные стойки ЦОД, холодный синеватый свет допустим как контраст | ⬜ ждёт блока |
| `support-photo` | Поддержка 24/7 | фото | 4:3, ~960×720 | живые люди в гарнитурах, не сток-улыбки, естественный свет | ⬜ ждёт блока |
| `review-avatars` | Отзывы | фото | 1:1, 88×88 (рендер 44px), 3 шт | сверстано с аватарами из hero (avatar-1..3.png) — при желании заменить уникальными | 🟡 врем. ассет |
| `eco-mail`, `eco-ssl`, `eco-builder`, `eco-ddos` | Экосистема (табы) | скриншот/иллюстрация | 4:3, ~640×480 | визуал услуги справа в табе (у «Доменов» — мок поиска, ассет не нужен) | ⬜ опционально |

Статусы: ⬜ ждёт блока → 🟡 заглушка сверстана, ждём ассет → ✅ ассет на месте.
